# Pinned dependency set for the portable Korean speech runtime.
#
# Captured from the project's Windows dev venv (.venv\Scripts\python.exe -m pip freeze)
# on 2026-09-13. These are the exact versions the runtime is assembled and tested with.
# prepare-speech-runtime.ps1 installs these with --only-binary=:all: into a fresh
# python\Lib\site-packages via pip --target (wheels only, no source builds, no
# user/global installs). This file is the portable build's dependency lock.
#
# ABI note: wheels are resolved by the builder interpreter (CPython 3.13, cp313-win_amd64)
# and are ABI-compatible with the 3.13.15 embeddable runtime (same 3.13 minor / cp313 tag).
anyio==4.15.1
av==18.1.0
certifi==2026.7.22
click==8.5.0
colorama==0.4.6
ctranslate2==4.8.2
faster-whisper==1.2.1
filelock==3.32.6
flatbuffers==25.12.19
fsspec==2026.7.0
h11==0.16.0
hf-xet==1.6.0
httpcore==1.0.9
httpx==0.28.1
huggingface_hub==1.31.0
idna==3.19
numpy==2.5.3
onnxruntime==1.30.0
packaging==26.3
protobuf==7.36.1
PyYAML==6.0.3
tokenizers==0.23.2
tqdm==4.70.1
typing_extensions==4.16.0
