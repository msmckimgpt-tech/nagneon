# 2차 실사용 대화 검토 · 2026-09-14

작업은 `codex/second-user-test-review`, `G:/dev/ai/00_game_backseat-worktrees/second-user-test-review`, 기준 `1031321ebedaca455d2b10a9e7f93a9f0f6bd1ff`에서 수행했다. 사용자 화면·장치·앱을 조작하지 않고, 허용된 `main/data`의 저널만 `TestDataReader`로 읽었다. 이 문서와 테스트에는 실제 대화 원문을 복사하지 않는다. [수집 경계](USER-TEST-COLLECTION.md), [갤러리·분리 음성 클립](GALLERY-AND-CLIP-VOICE.md)도 참고한다.

## 검토 범위와 해석

두 번째 세션 `f7c4af0a-4ccd-43f2-a672-f3939eef5fe7`은 2026-09-13 **20:47:40~23:18:54 KST**, 약 151분이다. 전체 **1,063행 / 209개 발언·응답 구간**과 교정 주석 **201건**을 읽었다. 스트리머 855행, 관객 208행이며 스트리머 행은 모두 마이크 전사 주석이 있다. 관객 메시지는 평균 25.99자, 12자 이하 24건, 웃음 표기 34건이다. 가까운 완전 일치 중복은 0건이지만 의미가 거의 같은 재반응은 있었다.

원본 인덱스 SHA-256은 `b6cbdb73631f484513f84ae3af56df405185235544e0907eec3089963a4f679b`, revision 1653이다. 분석과 재생 전후 원본 무변경을 확인했다. 개발 worktree의 `artifacts/second-test-audit.json`은 입력 파일 해시·집계, `artifacts/policy-replay-final.json`은 행 ID·교정 판정·검색 결과만 보관한다.

**201건은 오류 건수가 아니다.** 실제 음성이 남아 있지 않아 전사 정확도나 발언의 정답을 역산할 수 없다. 방송 중 개발 소스가 여러 차례 통합됐지만 실행 앱의 로드 버전이 저널에 남지 않았다. 지금 코드와 당시 앱을 동일 버전이라고 단정하지 않는다. 중계 요청 뒤의 긴 채팅과 사용자가 만족한 농담은 보존할 좋은 사례로 분류했다.

## 확인한 문제와 적용

| 근거: 세션 내부 행 번호 | 해석과 변경 |
|---|---|
| V16/17, V29/30, V222/224, V563/568, V664/666, V919/920 | 동일 관객의 가까운 시간 내 비슷한 재반응. 30초 내 동일 인물의 겹치는 표현 억제를 강화하고, 숫자·긍정/부정이 바뀐 새 발언은 보호한다. 모든 의미 중복을 판별하는 임베딩 검사는 아니다. |
| S116~121, 교정 주석 전체 | 의도 변경에 대한 사용자 불편. 기존 부정·숫자·질문·훈수 요청 보호에 취향·추측·농담·웃음·말투를 추가하고, 각 단어의 편집량도 제한한다. 원문은 불변이고 의심스러운 수정은 채팅에 쓰기 전에 거부한다. 현재 정책은 과거 201개 제안 중 164개를 허용·37개를 거부한다. 이는 정책 변화이며 정확도 측정이 아니다. |
| S425, `e7abef8c-24e3-4dc0-9021-dd5111edd197` | 짧은 토큰의 비정상적인 장기 반복 1건. 마이크 전사 반복을 대화 생성·후속 기억에서 제외하고 기존 재확인 UI로 전달한다. 원문 기록 및 앞뒤 정상 발언은 보존하고 키보드 입력의 반복은 막지 않는다. |
| S620~643, S644 | 약 44분의 발언 공백 뒤 마이크를 껐다는 설명이 있다. 관객의 화면 반응도 일부 있었다. 모든 무채팅을 결함으로 해석하지 않되, 최근 함께 나눈 대화가 있으면 정적 화면에서도 낮은 빈도의 잡담 기회를 준다. |
| S659~662, V664/666 | 장치 상태를 계속 확인하겠다는 관객의 근거 없는 약속. 관객에게 실제 제공되지 않는 마이크 감시 능력을 주장하지 않도록 명시했다. 새로운 물리 장치 감시 기능을 구현한 것은 아니다. |
| S784, V786 | 최신 발언에서 해결된 상황을 기다리는 반응. 명시적인 발견·성공·상황 번복이 오면 진행 중 일반 반응을 취소하고 보존된 최신 발언으로 다시 구성한다. 모든 서술마다 취소하지 않으며 호명·포인트 응답은 별도로 보호한다. |
| S796, V802 | 예전 추천자를 묻는데 목격한 추천이 검색에 빠짐. 복합 게임 명칭의 검색어와 추천 원문 우선 슬롯을 추가했다. 재생에서 V222 `be402197-31f3-4264-9468-f2b99483023f`, V224 `e4712a64-8e3a-4b73-9f3b-38acbe9c6f9f`가 당시 목격한 관객의 문맥에 포함됐다. 목격 경계 밖의 인물을 아는 것으로 만들지 않는다. |
| S145/164/197, S1016~1022 | 중계 요청과 농담·격려가 어울린 사례. 모든 긴 말을 제거하지 않고 명시적으로 요청된 중계는 허용한다. 최근 말투와 농담 맥락을 전달하되 성격 분석 결과를 공개하거나 관객을 똑같이 흉내 내게 하지 않는다. |

`streamer-expression.js`는 각 관객이 실제로 접한 최근 20분 발언을 읽어 8초 이내의 조각을 합친다. 최근 세 묶음과 농담·감사·선호 등이 있는 세 묶음을 원문 ID와 함께 전달한다. 장기 성격 진단을 저장하지 않으며 원문 삭제·부재 경계를 그대로 따른다.

정적 화면의 잡담은 마지막 공개 메시지 후 최소 60초, 최근 함께 들은 발언이 20분 내에 있고, 대기 채팅·조용히 해 달라는 요청이 없을 때만 후보가 된다. 빈 응답에도 75~135초의 재시도 간격을 두며 한 번에 최대 한 명이다. 새 화면 사건이나 소리를 만들지 않고 기존 장면을 덮어쓰거나 후원·클립을 자동 지급하지 않는다. 침묵 요청은 10분간 억제하고 명시적 대화 재개 요청은 이를 해제한다.

최종 검토 중 `artifacts/idle-expiry-before.json`에서 **화면 없는 잡담도 21초 응답 후 화면의 20초 만료로 버려지고 장면 상태가 지워지는 문제**를 재현했다. 잡담은 영상 참조와 그 만료 조건에서 분리했다. 실제 Studio·연속 프레임 경로를 사용하는 회귀 검사에서 늦은 잡담 한 개가 전달되고 이전 장면이 보존되는지 확인한다. 잡담 자체의 45초 만료와 새 발언 우선 처리는 유지한다.

## 검증과 실패 보존

`test/second-test-listening.test.js`는 교정의 의미·말투 보호, 이상 전사 격리, 삭제/목격 범위, 정적 화면과 침묵, 상황 번복, 중복과 추천 기억을 검사한다. 초기 집중 테스트의 5개 실패는 새 합성 fixture의 `supporters` 누락, 뒤의 1개 실패는 테스트 시계를 5초 호출 간격보다 적게 이동한 문제였다. fixture를 고친 뒤 47개 집중 검사가 통과했다. 원본 `listening-focused*.log`를 보존했다.

실제 공식 **gpt-6-astra / low**에는 실제 사용자 원문 대신 합성 사례만 보냈다. 최초 `listening-astra-Kteavj`의 요청 중계 사례가 출력 검증에 실패했다. `listening-astra-nDwphH/response-1.json`에서 로컬 메시지 제한 240자를 모델 출력 스키마가 명시하지 않은 원인을 확인했다. 모델 스키마에도 240자/최대 8개를 명시한 후 `listening-astra-2ASPLH/result.json`의 네 사례가 통과했다. 농담 8.468초, 조용한 잡담 9.802초, 요청 중계 14.413초, 해결된 상황 11.638초였다. 응답을 직접 읽어 농담 맥락·개인 취향·요청한 설명·최신 결과와 맞는지 확인했다.

이 네 사례는 작은 합성 표본이다. 물리 음성의 억양·감정·의도 정확도, 실제 게임 중 지연, 장기 잡담 반복률은 새 실사용으로 확인해야 한다. 실제 마이크 정답을 추정해 교정 성공률을 만들거나 모델 응답 시간을 종단 지연으로 표기하지 않는다. 최종 배포와 통합 검증은 아래 기록 및 [배포 문서](DISTRIBUTION.md)에 이어서 남긴다.

## 최종 배포 재실행

마지막 잡담 만료 수정 뒤 **519개 Node 검사, 실패·생략 0개와 TypeScript/Vite 빌드**가 통과했다. 원본은 `artifacts/second-review-final-canonical-check.log`, 좁은 재현/수정 검사는 `idle-expiry-before.json`과 `listening-idle-expiry-fix.log`다. 원본 데이터 인덱스 해시는 분석 이후에도 위와 동일했다.

최종 배포 ASAR를 자체 증거 폴더로 추출하고 **그 코드와 번들 공식 CLI**로 네 합성 대화 사례를 다시 요청했다. `artifacts/listening-astra-owpTOb/result.json`에서 playful-boast 8.271초, quiet-company 8.880초, requested-commentary 11.720초, resolved-outcome 9.183초였다. 응답 원문을 직접 검토하며 농담, 최근 취향, 요청 중계, 마지막 상황 정정과의 관계를 확인한다. 기계적 스키마 통과를 모든 문화·감정 품질의 합격으로 대신하지 않는다.

처음 만든 후보 `2026-09-13T15-39-45-486Z`도 인식기·모델·미디어 실행은 통과했지만, 검사 중 발견한 잡담 만료를 수정하면서 `server/studio.js`가 달라졌다. 소스 불일치를 잡은 `second-review-integrity.log`와 `candidate-package-integrity.json`, 전체 실패 상태인 `second-review-final-build-result.json`을 보존하고 후보를 최종본으로 사용하지 않았다. 변경 후 새 폴더를 만들고 전체 검증을 다시 실행한 결과가 `second-review-final-build-v2.json`이다.
