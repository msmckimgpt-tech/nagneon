# 경량 패키지의 설치·실행·복귀 검증

2026-09-23, Windows MCKIM. 기준 코드는 `1fc7f735ab8955cb7d9d30af62d132d2d729f4b9`이며 AI 대시보드와 리뷰 후속 개선을 포함한다. 이 기록은 **격리된 합성 프로필과 실제 Windows 패키지**의 검증이다. 사용자 실제 설치 적용이나 새 정식 릴리즈를 뜻하지 않는다.

## 결과

- `npm ci`, `npm run check`의 format·775개 테스트·TypeScript/Vite 빌드와 `npm audit --audit-level=high`를 통과했다. 취약점은 0건이다.
- 비표시 실제 Electron의 AI 대시보드 검사 7개를 통과했다. 대시보드 조회, 차단 정책, 기능 연결, 사용량 표시, 연결 해제, 420~1440px 화면을 합성 제공처로 검사했다.
- 새 기본 패키지는 34파일·795,635,258바이트다. 소스 112개와 파일별 해시·실행 보호 fuse가 일치하고 선택형 음성/GPU 파일의 불필요한 중간 복사 3,911,526,614바이트가 없다.
- ASAR SHA-256: `3be4b655f599daf0d7406c0738250ea94f0c7bb1e3ff5cce0c7a17831fc19119`.

| 단계 | 확인한 결과 |
|---|---|
| 기존 0.1.6 문화 기능 빌드 | 새 시험 프로필에 합성 제목·공통 기억을 저장하고 리허설 시작·중지·정상 종료 |
| 별도 설치 경로에 기존 빌드 설치 | 등록 옵션 없이 설치; 프로필 데이터 8개 파일의 전후 해시 일치 |
| 새 후보로 업데이트 | 같은 프로필과 별도 설치 경로 유지; 설치 직후 기존 8개 파일의 해시 일치 |
| 새 후보 첫 실행 | 실제 패키지 UI에서 저장한 제목·기억 확인, 리허설 시작·중지·정상 종료 |
| 새 후보 재시작 | 제목·기억 유지, 다시 시작·중지·정상 종료 |
| 기존 빌드로 복귀 | 설치 시점 프로필 10개 파일과 복귀 백업의 해시 일치; 실제 UI에서 제목·기억 유지와 시작·중지·정상 종료 |

네 차례 앱 실행은 각각 소유한 프로세스의 경로·프로필·부모 PID·생성 시각을 확인한 뒤 Windows `CloseMainWindow`로 정상 종료했다. 종료 코드와 해당 PID가 남지 않은 상태를 확인했다. 마이크·전체 화면·실제 방송 또는 모델 대화 검증은 포함하지 않는다.

## 범위와 주의점

`Install-NagneonRelease.ps1`은 작업 공간 안의 별도 설치 경로와 합성 프로필에 사용했고 `-Register`는 지정하지 않았다. 설치 조회도 같은 경로를 사용했다. 실제 사용자 설치 등록·바로가기·기록은 수정하지 않았다. 검증용 Windows PowerShell 프로세스에만 `RemoteSigned`를 지정했으며 이후 새 기본 프로세스의 정책은 기존 `Restricted`로 유지됐다.

구버전은 새 AI 대시보드의 차단 정책을 시행하지 않는다. 이번 복귀 검사는 제목·기억과 앱 실행의 보존을 확인하며 모든 새 기능의 구버전 호환을 보장하지 않는다. 실제 계정·OBS·물리 장치와 일반 Windows 탐색기의 고정 실행기 경로는 별도 수용 검증이다.

후보는 여전히 버전 0.1.6의 로컬 시험 빌드다. 기존 공개 태그나 파일을 덮어쓰지 않는다. 정식 릴리즈에는 새 버전과 해당 최종 패키지 검증·게시 해시가 필요하다.

## 증거

`review-package-acceptance-20260923/artifacts/`에 `native-acceptance-summary.json`, `check.log`, `audit.log`, `ai-dashboard.log`, `component-package-verification.json`, `baseline-native.log`, `candidate-native.log`, `candidate-restart-native.log`, `rollback-native.log`, 설치 3단계 로그와 프로필 해시 결과를 보존했다. 각 `packaged-ui/run-*/result.json`, `native.log`, `studio.png`가 실행별 원본이다.
