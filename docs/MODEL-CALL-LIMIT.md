# 모델 호출 한도 확대

2026-09-13 사용자 요청: 구독 사용량에 여유가 있으므로 서비스의 모델 호출 최대 제한을 넉넉하게 설정할 수 있게 한다.

- 기준 커밋: `4c634fca1683fc17e2331b083a199ac7652d9c8b`
- 작업: `feat/expanded-model-call-limit`, `G:/dev/ai/00_game_backseat-worktrees/expanded-model-call-limit`
- 설정 UI와 서버 검증 상한을 1,000에서 1,000,000으로 확대한다. 공통 JSON으로 같은 상한을 사용한다.
- 설정 → 연결·사용량에서 `넉넉하게 · 10만 회` 선택 후 저장하거나 1~1,000,000 정수를 직접 입력한다.
- 기존 저장값과 신규 프로필 기본값은 유지한다. 호출 카운터, 실패 호출 차감, 백오프, 계정 인증에는 변경이 없다. 서비스 한도와 제공처 계정 한도는 별개다.
- `npm ci` 성공. `npm run check`: Node 테스트 265/265, TypeScript 및 Vite 빌드 통과. 1,000회 초과 예약, 100,000회 경계, 스키마 범위 거절 및 HTTP 설정 반영을 검증했다.
- 원본 검증 로그: 작업 worktree의 `artifacts/model-call-limit-check.log`.
- 사용자 테스트 보호 지침에 따라 GUI/장치 테스트와 실행 중 앱 재시작은 하지 않았다. 실행 중 앱의 설정값도 변경하지 않았다. 사용자 앱 재시작 후 위 설정 저장이 필요하다. 원격 푸시는 요청 범위에 포함되지 않는다.
